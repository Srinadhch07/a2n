// Smooth scroll for nav links (optional future)
document.querySelectorAll('.nav-links a').forEach(link => {
  link.addEventListener('click', function(e) {
    e.preventDefault();
    alert('Navigation will work once full site pages are added!');
  });
});
